[workspace]
resolver = "2"
members = [
    "bin",
    "lib",
]

[profile.dev]
panic = "abort"

[profile.release]
codegen-units = 1
lto = true
# opt-level = "s" # size optimization
debug = true
panic = "abort"
